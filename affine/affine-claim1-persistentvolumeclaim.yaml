apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: affine-claim1
  name: affine-claim1
spec:
  storageClassName: nfs-client
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
